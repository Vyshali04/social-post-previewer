{
  "name": "quicksocial",
  "version": "1.0.0",
  "description": "AI Social Post Previewer - MERN Stack Application",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm run dev",
    "client": "cd frontend && npm start",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "keywords": ["mern", "social", "ai", "preview", "react", "node", "mongodb"],
  "author": "QuickSocial Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
